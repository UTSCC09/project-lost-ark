const typeDefs = `
  type Query {
    user: User
    coins: [Coin]
    coin(_id: String): Coin
  }
  type User {
    username: String!
    cash: Float!
    wallet: [Wallet]
  }
  type Coin {
    _id: ID
    symbol: String
    name: String
    price: Float
  }

  type Wallet {
    coin: Coin
    quantity: Float
  }

  type Mutation {
    signup(username: String!, password: String!): User
    
  }


`;

module.exports = { typeDefs };